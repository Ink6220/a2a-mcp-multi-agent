[pytest]
markers =
    requires_api_key: tests that contact external services (e.g. OpenAI, Nova)
addopts = -ra -q
testpaths =
    tests
norecursedirs =
    unit_tests
python_files =
    test_*.py
    *_tests.py 